{
    "name": "crk-publicity-portfolio",
    "version": "1.0.0",
    "private": true,
    "type": "module",
    "scripts": {
        "dev": "vite --host 0.0.0.0",
        "build": "vite build",
        "preview": "vite preview --host 0.0.0.0",
        "check": "node --test tests/*.test.js",
        "audit:security": "npm audit --audit-level=low && npm run check && npm run build"
    },
    "devDependencies": {
        "vite": "8.1.4"
    },
    "dependencies": {
        "@supabase/supabase-js": "2.110.3"
    }
}
